export interface EmployeeData {
  id: string;
  fullName: string;
}

export interface LeaveData {
  startDate: string;
  endDate: string;
}

export interface ChildData {
  firstName: string;
  lastName: string;
  birthDate: string;
}
